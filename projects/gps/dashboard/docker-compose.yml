version: "3.9"

services:
  gps-dashboard:
    image: registry.gitlab.com/amgad226/manage-fleet-pro/dashboard:dev
    container_name: gps-dashboard
    restart: unless-stopped
    ports:
      - "127.0.0.1:9100:80"
    env_file:
      - .env
